# BookMarkOS
A browser-native OS in a single data:text/html URI. Features a PowerShell style terminal virtual file tree and sandbox execution.  Setup: Download the .txt file, copy its contents, create a new browser bookmark, paste the code into the URL field, and name it Bookmark OS. Tap it to launch, then type help for commands. (Tested on the Chrome browser).
